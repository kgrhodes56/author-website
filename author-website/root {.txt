:root {
  --bg: #050608;
  --bg-alt: #111318;
  --text: #f5f5f5;
  --muted: #c7c7c7;
  --accent: #c45b32; /* copper red */
  --accent-soft: #8b3a24;
  --border: #2a2d35;
  --font-main: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
}

* {
  box-sizing: border-box;
}

body {
  margin: 0;
  font-family: var(--font-main);
  background: radial-gradient(circle at top, #1a1c24 0, #050608 55%);
  color: var(--text);
}

/* Layout */

header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 1.25rem 8vw;
  border-bottom: 1px solid var(--border);
  background: rgba(5, 6, 8, 0.9);
  backdrop-filter: blur(8px);
}

.logo {
  font-weight: 700;
  letter-spacing: 0.12em;
  text-transform: uppercase;
  color: var(--accent);
}

nav a {
  margin-left: 1rem;
  text-decoration: none;
  color: var(--muted);
  font-size: 0.95rem;
}

nav a.active,
nav a:hover {
  color: var(--accent);
}

.hero {
  padding: 4rem 8vw 3rem;
  text-align: left;
}

.hero-content {
  max-width: 720px;
}

.hero h1 {
  font-size: 2.4rem;
  margin-bottom: 0.75rem;
}

.hero p {
  font-size: 1.05rem;
  color: var(--muted);
  margin-bottom: 1.5rem;
}

.highlight {
  padding: 2.5rem 8vw;
  background: linear-gradient(to right, rgba(196, 91, 50, 0.12), transparent);
  border-top: 1px solid var(--border);
  border-bottom: 1px solid var(--border);
}

.highlight h2 {
  margin-bottom: 0.5rem;
}

.highlight p {
  max-width: 720px;
  color: var(--muted);
}

.grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
  gap: 1.75rem;
  padding: 2.5rem 8vw 3rem;
}

.grid-item {
  padding: 1.5rem;
  border: 1px solid var(--border);
  border-radius: 8px;
  background: rgba(17, 19, 24, 0.9);
}

.grid-item h3 {
  margin-top: 0;
  margin-bottom: 0.5rem;
}

.grid-item p {
  color: var(--muted);
}

.content {
  padding: 3rem 8vw 3.5rem;
  max-width: 900px;
  margin: 0 auto;
}

.content h1 {
  margin-top: 0;
  margin-bottom: 1rem;
}

.intro {
  color: var(--muted);
  margin-bottom: 2rem;
}

/* Books */

.book-list {
  display: grid;
  gap: 2rem;
}

.book {
  display: grid;
  grid-template-columns: 140px 1fr;
  gap: 1.5rem;
  padding: 1.5rem;
  border-radius: 8px;
  border: 1px solid var(--border);
  background: rgba(17, 19, 24, 0.9);
}

.book-cover {
  width: 140px;
  height: 210px;
  border-radius: 4px;
  border: 1px solid var(--border);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--muted);
  font-size: 0.85rem;
  text-transform: uppercase;
}

.book-info h2 {
  margin-top: 0;
  margin-bottom: 0.5rem;
}

.book-info p {
  color: var(--muted);
}

.meta {
  font-size: 0.9rem;
  margin-top: 0.75rem;
}

.links {
  margin-top: 1rem;
}

/* Bio */

.bio-text p {
  margin-bottom: 1rem;
  color: var(--muted);
}

/* Contact */

.contact-block {
  margin-bottom: 2rem;
}

.contact-block h2 {
  margin-bottom: 0.5rem;
}

.contact-block p,
.contact-block ul {
  color: var(--muted);
}

.contact-block ul {
  padding-left: 1.2rem;
}

/* Buttons and links */

.btn-primary,
.btn-secondary,
.link {
  display: inline-block;
  padding: 0.55rem 1.1rem;
  border-radius: 999px;
  font-size: 0.9rem;
  text-decoration: none;
}

.btn-primary {
  background: var(--accent);
  color: #fff;
}

.btn-primary:hover {
  background: var(--accent-soft);
}

.btn-secondary {
  border: 1px solid var(--accent);
  color: var(--accent);
}

.btn-secondary:hover {
  background: rgba(196, 91, 50, 0.12);
}

.link {
  color: var(--accent);
}

/* Footer */

footer {
  padding: 1.5rem 8vw 2rem;
  border-top: 1px solid var(--border);
  text-align: center;
  font-size: 0.85rem;
  color: var(--muted);
}

/* Responsive */

@media (max-width: 720px) {
  header {
    flex-direction: column;
    align-items: flex-start;
  }

  nav {
    margin-top: 0.75rem;
  }

  .hero {
    padding-top: 3rem;
  }

  .book {
    grid-template-columns: 1fr;
  }

  .book-cover {
    margin-bottom: 0.75rem;
  }
}
